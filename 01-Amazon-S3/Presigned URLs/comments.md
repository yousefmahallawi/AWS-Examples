# Notes

A Presigned URL temporarily grants access to a private object.

The URL contains a cryptographic signature generated using AWS Signature Version 4.

After the expiration time passes, the URL automatically becomes invalid.
